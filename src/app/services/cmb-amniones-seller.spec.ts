import { CmbAmnionesSeller } from './cmb-amniones-seller';

describe('CmbAmnionesSeller', () => {
  it('should create an instance', () => {
    expect(new CmbAmnionesSeller()).toBeTruthy();
  });
});
